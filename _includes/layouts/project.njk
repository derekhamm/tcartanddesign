---
layout: layouts/base.njk
section: project
---

<article>
  <h1>{{ title }}</h1>
  <img src="{{ thumbnail }}" />

  {{ layoutContent | safe }}

  {% if tags %}
    <p>
    {% for tag in tags %}
      {%- if tag != "project" -%}
        {% set tagUrl %}/tags/{{ tag }}/{% endset %}
        <a href="{{ tagUrl | url }}" rel="tag">{{ tag }}</a>
      {%- endif -%}
    {% endfor %}
    </p>
  {% endif %}

</article>

<nav>
  <a href="{{ '/portfolio/' | url }}">← All Projects</a>
</nav>